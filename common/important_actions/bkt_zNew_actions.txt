#War dec thingy

#action_empty_council_position
action_war_declaration_powerful_vassal_approval_denied = {
	#type = action
	combine_into_one = yes
	is_dangerous = yes
	
	check_create_action = {
		if = {
			limit = {
				highest_held_title_tier > tier_barony
				has_realm_law_flag = war_declaration_powerful_vassal_approval
			}
			every_vassal = {
				limit = {
					is_powerful_vassal = yes
					age >= 16
					opposes_succession_law_change_trigger = yes
				}
				try_create_important_action = {
					important_action_type = action_war_declaration_powerful_vassal_approval_denied
					actor = root
					recipient = this
				}
			}
		}
	}
	effect = {
		#scope:actor = {
		#	open_view_data = {
		#		view = council_window
		#	}
		#}
		scope:recipient = {
			open_view_data = {
				view = character
			}
		}
	}
	
	#unimportant = {
	#	can_declare_war_according_to_laws_trigger = yes
	#}
}

action_war_declaration_council_approval_denied = {
	#type = action
	combine_into_one = yes
	is_dangerous = yes
	
	check_create_action = {
		if = {
			limit = {
				highest_held_title_tier > tier_barony
				has_realm_law_flag = war_declaration_council_approval
			}
			every_councillor = {
				limit = {
					NOR = {
						prev.cp:councillor_spouse ?= this
						prev.cp:councillor_vizier ?= this
					}
					opposes_succession_law_change_trigger = yes
				}
				try_create_important_action = {
					important_action_type = action_war_declaration_council_approval_denied
					actor = root
					recipient = this
				}
			}
		}
	}
	effect = {
		scope:actor = {
			open_view_data = {
				view = council_window
			}
		}
	}
	
	#unimportant = {
	#	can_declare_war_according_to_laws_trigger = yes
	#}
}